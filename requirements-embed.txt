# [FIND-1 S2] 임베딩 계산 전용 의존성 -- grm-findings-embed.yml 워크플로에서만 설치한다.
# ★requirements.txt(매일 도는 intake CI)에는 절대 추가하지 마라 -- 그러면 매일 수집
# 워크플로가 torch 를 받게 된다(현행 requirements.txt 는 requests/PyMuPDF/Jinja2 뿐).
#
# CPU 전용 torch 휠을 쓰도록 PyTorch 공식 CPU 인덱스를 추가 지정해 다운로드 크기를
# 줄인다(GPU/CUDA 휠 대비 수 GB 절감). sentence-transformers 가 huggingface_hub·
# transformers·numpy·torch 를 함께 끌고 온다(설계 실측: 모델 fp32 ≈ 471MB -- CI 시간의
# 주 요인이므로 워크플로에서 pip + huggingface 캐시를 반드시 병행한다).
--extra-index-url https://download.pytorch.org/whl/cpu

# ★F-05(공급망 pin, Codex 감사) -- floating range 대신 정확 버전 고정. 추측이 아니라
# 성공한 실행(run 29406523575, 2026-07-16 조회)의 pip install 로그에서 실측한 값이다
# (torch 휠 = torch-2.13.0+cpu-cp312). huggingface_hub 는 resolver 가 1.23.0 에서
# backtrack 해 최종 선택한 값 -- 명시 pin 이 그 backtracking 자체를 없애 설치도 빨라진다.
#
# ★이 파일은 **소스 spec(.in 역할)**이다 -- 워크플로가 실제로 설치하는 것은
# `requirements-embed.lock`(전이 의존성 전체 + sha256, `pip install --require-hashes`)이다.
# F-05 완결: 직접 의존성 6종 pin 만으로는 전이 의존성이 floating 이라 원 finding 의
# 공급망 위험이 남았다(Codex 후속 감사 "부분 종결") -> lock 도입으로 닫는다.
# 버전을 올릴 때: 아래 == 를 고치고 `grm-findings-embed-lock.yml` 을 workflow_dispatch 로
# 실행해 lock 을 재생성한 뒤 함께 커밋한다(lock 은 ubuntu/cp312 러너에서 해상해야 정확).
#
# requests 는 findings_embed_service.py 가 쓴다. embed job 이 **lock 하나로만** 설치하도록
# 여기에 포함한다(requirements.txt 를 따로 설치하면 그 경로가 hash 미검증으로 남는다).
# ★그래도 requirements.txt(매일 도는 intake) 자체는 건드리지 않는다 -- 위 경고 유지.
requests==2.34.2
torch==2.13.0
sentence-transformers==3.4.1
transformers==4.57.6
huggingface_hub==0.36.2
numpy==2.5.1
