# 신규 소스 타당성·판정 — FDA 수입경보 · FDA Data Dashboard · EDQM CEP

| 메타 | 값 |
|---|---|
| 작성일 | 2026-08-11 |
| 대상 시스템 | GRM `origin/main` `79bfce2`(#678 머지 후) |
| 성격 | 판정(미구현). 실측 근거는 전부 이 세션에서 직접 호출해 얻은 값이다 |
| 연관 | `GRM_SYSTEM.md` §3.4(소스표)·§6.2(잔여 작업) · `신규소스_EudraGMDP_EDQM_타당성_2026-06-11.md`(선행 조사) |
| 병행 트랙 | 같은 시점 별도 세션이 Health Canada 실사 수집기·WHOPIR PDF 파싱을 진행 중 |

---

## 0. 한 줄 결론

**수입경보 = 채택(조건부)** · **FDA Data Dashboard = 보류(무인증 경로 부재 확정, 키 신청이 선행)** · **EDQM CEP = 수집 불가(2026-08-11 오후 정정 — 아래 ⚠️)**.

> ⚠️ **2026-08-11 오후 정정.** 이 문서의 최초 판정은 EDQM CEP = "채택, 접근성 해결됨"이었다. **틀렸다.** 접근성을 **curl** 로 측정하고 **`requests` 로 돌 코드**를 판정한 것이 원인이다(§4.3). 같은 머신·같은 순간·같은 UA 로 curl 은 200, `requests` 는 403 Cloudflare 챌린지다. 정정된 판정과 근거는 §4 전체를 다시 읽을 것. **이 함정을 다시 밟지 않도록 §4.3 을 먼저 보라.**

---

## 1. 배경 — 왜 지금 이 셋인가

2026-08-11 실측 기준 findings 코퍼스는 7개 레인이고 FDA·MFDS 두 축이 97%다.

| 레인 | findings | 문서 | 커버리지 |
|---|---:|---:|---|
| FDA 483 | 11,489 | 1,932 | 2016-11 ~ |
| FDA 경고서한 | 3,397 | 1,303 | 2021-01 ~ |
| MFDS 회수 | 953 | 953 | 2023-08 ~ |
| MFDS GMP 실사 | 926 | 249 | 2023-05 ~ |
| MFDS 행정처분 | 140 | 140 | 2024-05 ~ |
| EU GMP NCR | 75 | 75 | 2019-01 ~ |
| MHRA GMP NCR | 6 | 6 | 2020-02 ~ |

EU NCR 75·MHRA 6은 **원천 전량**이라 더 팔 곳이 없다(연 20건이 유럽 전체 발행량). MFDS는 오픈API 65개 전수 확인으로 findings급 채널이 더 없음이 확정됐다. 즉 **남은 확장은 새 채널을 여는 것뿐**이고, 그 후보를 판정한 것이 이 문서다.

---

## 2. FDA 수입경보 (Import Alert 66-40 · 66-41) 〔채택 — 조건부〕

### 2.1 무엇
제조소 단위 **무검사 자동억류(DWPE, Detention Without Physical Examination)** 목록. GRM이 가진 다른 모든 소스가 "과거에 일어난 사건"인 데 반해, 수입경보는 **오늘 현재의 상태**("이 제조소는 지금 미국 수입이 막혀 있다")를 말하는 유일한 채널이다.

### 2.2 접근성·구조 (2026-08-11 실측)
- `https://www.accessdata.fda.gov/cms_ia/importalert_189.html` = IA **66-40** (1.9 MB, HTTP 200)
- `https://www.accessdata.fda.gov/cms_ia/importalert_190.html` = IA **66-41** (6.7 MB, HTTP 200)
- 한국 IP에서 정상 수신. `www.fda.gov` 최상위 도메인과 달리 Akamai 봇차단에 걸리지 않았다.
- **`<table>` 0개.** 업체 1건 = `<div class="div-info">` 블록 하나:
  - `<div class="div-name floatleft">` 회사명
  - `<div class="div-name floatright">Date Published : MM/DD/YYYY</div>`
  - `<div class="clear">` 주소, 도시, 국가
  - 이어서 제품코드마다 별도 `<div>`(클래스 없음) + 자체 `Date Published:`
- 두 알림 모두 **Red List만 존재**(Green/Yellow List 앵커·문자열 0건).

### 2.3 볼륨·한국 관련성
| | 업체 수 | 한국 등재 |
|---|---:|---:|
| IA 66-40 | 465 | 37 |
| IA 66-41 | 1,920 | 94 |
| 합계(정규화 중복제거) | 2,163 | 115 |

- 66-40 상위국: 중국 156 · 인도 108 · **한국 37** · 캐나다 27 · 멕시코 17
- 66-41 상위국: 인도 286 · 캐나다 242 · 중국 234 · 멕시코 99 · **한국 94**
- 등재된 국내 업체에 **대형사가 다수 포함**된다(COSMAX, COSRX, LG H&H, 아모레퍼시픽 데일리뷰티 플랜트, 한국콜마, 휴젤, 메디톡스, 휴온스, 녹십자 음성공장 등). 사내 사용자에게 즉각적 관련성이 있다.

### 2.4 채택 근거 — 두 가지 결정적 실측
착수 전 반드시 확인해야 했던 두 질문에 대한 답이 모두 긍정이었다.

**(1) 날짜 필드가 있는가 → 있다.**
`Date Published (MM/DD/YYYY)`가 업체 레벨 **100%**(465/465, 1920/1920), 제품코드 레벨에도 개별 존재. 그중 66-40은 62.4%, 66-41은 27.8%가 **업체 헤더 날짜와 다른 값**이다 — 즉 이 날짜는 형식적 텍스트가 아니라 실제 추가 시점을 반영한다. **"이번 주 신규 등재"를 만들 수 있다.** 이 필드가 없었다면 주간 브리프에 실을 수 없어 채택 근거가 무너졌을 것이다.

**(2) 이미 아는 정보인가 → 아니다.**
findings의 `firm_name`(distinct 3,166)과 정규화 대조한 중복도는 **전체 7.3% · 한국만 14.8%**. 즉 들여올 정보의 **85~93%가 현재 DB에 없는 업체**다. `finding_text`의 `import alert` 언급도 16,986건 중 17건(0.1%)뿐이고, 그 17건조차 DEG/EG 오염 관련 별개 경보를 가리킨다 — 지금 483/경고서한 경로로 대체 커버되고 있다는 증거는 없다.

### 2.5 반대 근거(무시하지 말 것)
- **정보 깊이가 얕다.** 업체명·주소·제품코드·날짜뿐이고 위반사유 서술이 없다 → `finding_text`를 채울 실질 내용이 없다. **기존 findings 스키마에 그대로 끼워 넣으면 스키마 목적과 어긋난다.**
- **해제일 필드가 없다.** '해제됨'은 스냅샷 diff로만 추정 가능하고, 수집을 한 번이라도 건너뛰면 그 사이 해제/재등재 이력이 영영 유실된다. (일일 크론이 실제로 며칠 침묵한 전례가 있다.)
- **파서가 취약하다.** 필드 구분이 `div-name floatleft/floatright` 같은 **프레젠테이션 클래스뿐**이고 의미론적 마크업(`itemprop`, `data-*`)이 전혀 없다. FDA CMS 리디자인 시 조용히 깨진다.
- **문서 내부 표기 불일치.** 66-40에 13건, 66-41에 65건의 중복명(같은 업체·다른 대소문자/구두점/법인격 접미어). **정규화 없이 문자열 diff를 돌리면 같은 업체가 매주 '신규'로 오탐된다.**
- **갱신 주기 미검증.** HTTP `Last-Modified: 2026-08-07` 단일 스냅샷만 봤다. 실제 갱신 주기는 1~2주 뒤 재수집 diff로 확인해야 한다.

### 2.6 착수 지시 (요약)
1. **findings 테이블에 넣지 마라.** 별도 경량 리스트형 테이블(업체·주소·국가·제품코드·등재일·알림번호)을 신설한다. 서술형 스키마와 목적이 다르다.
2. 2.5의 정규화 로직(대문자화 → 영숫자 외 공백 치환 → 법인격 접미어 토큰 제거 → 연결)을 **파이프라인에 필수로 내장**한다. 문서 내부 중복부터 정리되지 않으면 신규 탐지가 성립하지 않는다.
3. **스냅샷 원본을 보관**한다. 해제일이 없으므로 스냅샷이 유일한 이력이다.
4. 발행(브리프 카드·페이지)은 **최소 2주 재수집 diff로 갱신 주기를 확인한 뒤** 연다. 수집·보관을 먼저 시작해야 그 검증이 가능하다.
5. 범위는 이 두 알림에 한정한다. FDA 수입경보는 90개 이상이고 전량 확장 시 물량이 findings(16,986)를 압도한다.

---

## 3. FDA Data Dashboard (실사 등급·조항 인용 통계) 〔보류 — 키 신청 선행〕

### 3.1 무엇·왜 원했나
GRM은 현재 **분자만** 갖고 있다("지적 11,489건"). 실사 자체의 모수(실사 건수, 등급 NAI/VAI/OAI, 조항별 인용 통계)가 없어 "많은지 적은지"를 말할 기준이 없다. 이걸 붙이면 "실사 M건 중 N건", "업종별 OAI 비율"을 말할 수 있게 된다.

### 3.2 판정: **무인증 경로는 존재하지 않는다** (4개 후보 전수 실측)
| 후보 | 결과 |
|---|---|
| 정적 다운로드 파일(`datadashboard.fda.gov/InspectionsDataset.xlsx` 등) | **404**(진짜 IIS 오류, 봇차단 아님). HTML의 `href`는 죽은 폴백이고 실제 동작은 `onclick="exportData(...)"`가 가로챈다 — Qlik Sense JS 엔진의 **살아있는 WebSocket 세션**이 있어야만 내려받아진다. curl로는 원천적으로 불가 |
| 배후 REST API `api-datadashboard.fda.gov/v1/*` | 엔드포인트 실재. 필드 검증까지 통과시킨 유효 요청에도 **401 Not Authorized** 확정. `Authorization-User`/`Authorization-Key` 필수 |
| openFDA (`api.fda.gov`) | `download.json` 카탈로그 전수 확인 — 실사·인용·수입거부 엔드포인트가 **애초에 없다** |
| 레거시 `accessdata.fda.gov/scripts/inspsearch/` | **404**(폐기) |

### 3.3 사람이 해야 할 일 (사용자 액션)
**OII Unified Logon**으로 Data Dashboard API 키를 신청한다. 필요 정보는 이메일·이름·성·소속기관이며, openFDA처럼 즉시 자율발급이 아니라 **승인 대기형**이다. 안내 페이지: `https://datadashboard.fda.gov/oii/api/index.htm`

> ⚠️ **openFDA 키와 별개 시스템이다.** 이미 `OPENFDA_API_KEY`를 갖고 있어도 이 API에는 통하지 않는다.

### 3.4 주의
`www.fda.gov` 최상위 도메인은 Akamai 봇탐지로 비브라우저 UA를 차단한다(실측: 404 → `abuse-detection-apology.html`로 리다이렉트). 이 트랙을 자동화에 넣을 때 fda.gov 페이지 스크래핑 단계마다 걸린다.

---

## 4. EDQM CEP Actions 〔**수집 불가** — 2026-08-11 오후 정정〕

### 4.1 미착수 사실
`신규소스_EudraGMDP_EDQM_타당성_2026-06-11.md` §3에서 **"채택 권고, 2순위, 난이도 낮음"**으로 판정됐고 §4에서 도입 순서 2번으로 지정됐다. 1순위였던 EudraGMDP NCR은 구현·라이브됐으나(소스 14), **EDQM CEP는 두 달이 지나도록 코드가 존재하지 않는다**(`collect_edqm_cep.py` 부재, 저장소 전수 grep 결과 EDQM 참조 0건).

### 4.2 무엇 — 왜 다른 소스와 겹치지 않는가
유럽약전 적합성인증(CEP)의 **정지·철회·복원** 공개 목록. CEP는 **원료(API)**가 약전 모노그래프에 적합함을 인증하는 것이라, 정지/철회는 곧 **원료 공급 적합성 리스크**다.

GRM의 기존 7개 레인은 전부 **제조소(사이트) 관점**이다. EDQM만 **원료·물질 관점**이라 구조적으로 겹치지 않는다. EudraGMDP NCR과는 상보 관계이고, EDQM 페이지 자체가 "Read the current Non-Compliance Reports" 링크로 EudraGMDP를 가리켜 두 소스의 연계가 원문에서 확인된다.

국내 제약사에 직접적이다 — 원료를 어디서 받는지가 곧 리스크이기 때문이다.

### 4.3 접근성 — ★측정 도구가 결론을 정한 오판 (2026-08-11 오전 → 오후 정정)

**오전의 틀린 판정**: `curl` 로만 재고 "기본 UA 403 × 5/5 · 브라우저 UA 200 × 5/5 = `UA_GATED`, 이 저장소가 이미 쓰는 방식이라 새로 만들 것 없음"이라고 결론냈다.

**오후 실측 — 같은 머신·같은 순간·같은 UA 문자열**:

| 클라이언트 | 결과 |
|---|---|
| **curl** (브라우저 UA) | **HTTP 200** · 225,976 bytes |
| **python `requests`** (같은 UA) | **HTTP 403** · `cf-mitigated: challenge` · `Server: cloudflare` · 본문 `Just a moment...` |

**즉 UA 문자열 게이트가 아니라 HTTP 클라이언트(TLS 지문) 단위의 봇 방어다.** UA 를 무엇으로 바꿔도 `requests`/`urllib` 로는 못 뚫는다(브라우저 UA·확장 헤더 세트·urllib 직접 호출 3가지 방법 전부 재확인). 수집기는 `requests` 로 도는데 **판정은 curl 로 했다** — 측정에 쓴 호출 형태가 결론을 정한 전형적 오판이다.

**공식 RSS 도 동일하게 막힌다.** EDQM 이 기계 소비용으로 발행하는 CEP 뉴스룸 피드(`/en/newsroom-cep/-/asset_publisher/cUV3HrUHZ3lm/rss`)와 전체 뉴스룸 피드 둘 다 `requests` 에 403 Cloudflare 챌린지를 준다. 즉 **스크래핑만의 문제가 아니라 도메인 전체 정책**이다.

**robots.txt 가 AI 크롤러를 이름으로 전면 차단한다**:
```
User-agent: ClaudeBot          → Disallow: /
User-agent: GPTBot             → Disallow: /
User-agent: CCBot              → Disallow: /
User-agent: Google-Extended    → Disallow: /
User-agent: Bytespider         → Disallow: /
User-agent: meta-externalagent → Disallow: /
Content-Signal: search=yes, ai-train=no, use=reference
```
(`User-agent: *` 의 Disallow 목록에 `/en/actions-on-ceps` 자체는 없지만, 위 명시적 차단 목록과 활성 기술 조치를 합치면 운영자의 의사는 분명하다.)

### 4.4 판정 — 만들지 않는다

뚫는 방법은 **TLS 지문 위장**(curl_cffi·tls-client 류)이나 **헤드리스 브라우저 자동화**뿐이다. **둘 다 만들지 않는다** — 이 저장소는 FDA 483 Akamai 차단 때 이미 같은 판단을 내렸고(`grm-fda483-akamai-block-json-backbone`: "TLS위장 우회는 안 만듦"), 봇 탐지 우회는 사이트 운영자가 명시적으로 표명한 의사를 거스르는 일이다.

**★재조사 금지.** 위 실측은 결정적이다. "UA 를 바꿔보자"·"헤더를 더 넣어보자"는 이미 전부 시도해 실패했다.

### 4.5 그래도 남겨 둔 것 — 파서는 검증이 끝났다

브랜치 **`feat/edqm-cep-actions`**(머지하지 않음)에 `collect_edqm_cep.py` 의 순수 파서 `parse_cep_actions()` 와 실측 스냅샷 픽스처, **32개 테스트(전부 통과)** 가 있다. 네트워크 0 으로 돈다.

파서가 흡수하는 원문 함정 4종(전부 실측):
- 섹션 마커에 `Looking for a CEP?` 가 섞인다 → 화이트리스트 3종만 인정
- 정지 섹션은 `fulfill`, 철회 섹션은 `fulfil` — 사유 정확일치 매칭이면 한쪽이 통째로 샌다
- 물질명에 HTML 엔티티가 붙는다(`Diosmin&nbsp;`)
- 빈 표는 `- | - | -` 한 행 → 0건 처리

**되살리는 조건**: EDQM 에 데이터 접근을 문의해 허락받거나(헬프데스크 존재), 공식 배포 채널이 열리면 **입력 경로만 갈아끼우면 된다.** 배선(`collect_intake`·`card_scaffold`·`findings_extractors`·워크플로)은 **일부러 하지 않았다** — 가져올 수 없는 소스를 배선하면 "켜져 있는데 조용히 죽어 있는 슬롯"이 된다.

### 4.6 실측 데이터 정정
2026-08-11 스냅샷 기준 19행(2026-02-16 ~ 07-27, 약 5.5개월 **롤링 창** — 지나간 항목은 사라지므로 **과거분 백필이 원천적으로 불가능**하다).
- [정지] 보유자 요청 3건 · **GMP 비준수 14건**(최초 기재 12건은 오기) · 인증절차 미충족 0건
- [철회] GMP 비준수 0건 · 인증절차 미충족 2건 · 모노그래프 삭제 0건
- [복원] 0건

---

## 5. 왜 이번 세션에서 구현하지 않았는가

사유가 셋 다 다르다.

- **EDQM CEP** — 착수했다가 **중단**했다. 수집 자체가 불가능한 것이 실측으로 확정됐다(§4.3~4.5). 검증된 파서만 브랜치에 보존하고 배선은 하지 않았다.
- **수입경보** — **갱신 주기가 미검증**이다(단일 스냅샷의 `Last-Modified` 만 봤다). 최소 1~2주 재수집 diff 로 확인한 뒤 발행을 열어야 한다.
- **FDA Data Dashboard** — **사람의 키 신청이 물리적 선행조건**이다.

부수적으로, 새 intake 소스는 `collect_intake.py`·`card_scaffold.py`·`findings_extractors.py`·`.github/workflows/grm-intake.yml` 을 건드리는데 같은 시점에 별도 세션이 Health Canada 실사·WHOPIR 로 정확히 그 파일들을 만지고 있었다(이 저장소는 병렬 세션이 같은 파일을 만져 사고가 난 전례가 있다).

수입경보·FDA Data Dashboard 는 착수 전 최대 미지(날짜 필드·중복도·무인증 가능성)를 이 문서가 실측으로 해소했으므로 다음 세션이 조사 없이 구현에 들어갈 수 있다. **EDQM 은 그 반대다 — 실측이 "하지 말 것"을 말한다.**

---

## 6. 채택 시 갱신할 `GRM_SYSTEM.md` 섹션

| 섹션 | 갱신 |
|---|---|
| §3.4 소스표 | 수입경보 행 추가(글로벌 확장, opt-in). **EDQM 은 추가하지 않는다** |
| §3.4 "검토 후 제외" | FDA Data Dashboard = 제외가 아니라 **키 대기**임을 명시(TGA/PMDA와 성격이 다르다) · **EDQM = 봇 방어로 수집 불가**를 TGA(WAF 차단)와 같은 줄에 등재 |
| §5.1 폴더 구조 | 수입경보 수집기 등록(EDQM 은 미머지 브랜치라 트리에 없다) |
| §5.3 플래그 | `ENABLE_FDA_IMPORT_ALERT`(기본 off) |
| §6.2 잔여 작업 | 수입경보·FDA Data Dashboard 를 OPEN 으로 등재. EDQM 은 **CLOSED(수집 불가)** 로 등재하고 브랜치 이름을 남긴다 |

---

## 7. 부정 판정 (재조사 불필요)

| 후보 | 판정 | 근거 |
|---|---|---|
| TGA | 제외 유지 | 2026-08-11 재확인 — curl 연결 자체 실패(`000`). WAF 차단 |
| **EDQM CEP** | **수집 불가** | 2026-08-11 오후 — Cloudflare 가 **HTTP 클라이언트 단위**로 막는다(curl 200 / `requests` 403 `cf-mitigated: challenge`). **공식 RSS 도 동일**하게 403. robots.txt 가 ClaudeBot·GPTBot 등을 이름으로 전면 차단(`ai-train=no`). 우회는 TLS 위장·헤드리스뿐이라 만들지 않는다 → §4. 검증된 파서만 브랜치 `feat/edqm-cep-actions` 에 보존(미머지) |
| PMDA per-event | 제외 유지 | 공개 per-event 결함 피드 없음·일본어 전용. 자료실 15건으로 완결 |
| MFDS 추가 채널 | **천장 확인** | 오픈API 65개 전수 확인, findings급 채널 없음 |
| EU GMP NCR / MHRA GMP NCR 증량 | **천장 확인** | 75건·6건이 원천 전량. 연 20건이 유럽 전체 발행량 |

---

## 📝 변경 이력
| 날짜 | 변경 내용 |
|---|---|
| 2026-08-11 | 최초 작성. 수입경보=채택(조건부)·FDA Data Dashboard=보류(무인증 경로 부재 확정)·EDQM CEP=채택(6월 권고 미착수 발굴 + 접근성 UA_GATED 실측). 부정 판정 4건 재확인 |
| 2026-08-11 (오후) | **EDQM CEP 판정 정정: 채택 → 수집 불가.** 구현 착수 후 하위 에이전트가 라이브 재현에 실패해 재측정한 결과, 오전의 `UA_GATED` 판정이 **curl 로 재고 `requests` 로 돌 코드를 판정한 오판**이었음이 드러났다(같은 UA 로 curl 200 / `requests` 403 Cloudflare 챌린지). 공식 RSS 도 동일 차단, robots.txt 의 AI 크롤러 전면 차단까지 확인해 §4 를 전면 개정하고 §0·§6·§7 을 정합화. 실측 데이터 카운트 1건 정정(GMP 비준수 정지 12→14). 검증된 파서는 미머지 브랜치에 보존 |
